PR #111 合并前阻塞项修复（已审查确认，按杠杆排序）

第一步：CI 假绿三连修（.github/workflows/ci.yml）
- GUT 命令加 -gdisable_colors；grep 断言改为可匹配的无色汇总行（"Failing Tests" 后跟非零值）
- gut_exit 非 0/非 124 即 exit 1
- 新增 grep 'SCRIPT ERROR|Parse Error|Ignoring script' 即失败
- 新增最小规模断言：Scripts ≥ 95、Tests ≥ 1700

第二步：缓存正确性
- cache_revision_index.gd：configure_android_export 补 resource:res://export_presets.cfg + RESOURCE_AGGREGATE；create_scene/save_scene/save_branch_as_scene 补 RESOURCE_AGGREGATE（batch_scene_node_edits 的 save=true 同理）
- script_compile_memo.gd 加 static invalidate(path)（按路径前缀清条目）；modify_script/create_script 写盘后调用
- 单测覆盖：上述标签推进使对应读失效；invalidate 后重算

第三步：completed 诚实性
- mcp_debugger_bridge.gd：错误类输出事件不随 500 环淘汰（独立无界/大容量环）
- game_workflow_engine.gd result_passed：stale/from_cache 判不通过；verify_scripts 拒 truncated（script_tools_native.gd 补 truncated 字段）；modify_script 带 validation.error_count>0 判修复失败；expect_fail 要求形状合法的负判决
- game_workflow_tools.gd _requeue_profile_evidence："completed"→"done"；VOLATILE_FAILURE_KEYS 增加 diff_pixel_count/diff_ratio/rmse/max_channel_delta
- 视觉基线命名按 workflow_id 区分（不再全目标共用 mcp_runtime_capture.jpg 基线）

第四步：平台导出正确性
- game_workflow_engine.gd release_export：按 _normalize_platform 结果映射预设名/平台/导出扩展（web→Web/index.html 等），engine spec 与 game_workflow_tools derive 同源
- export_preset_tools.gd：if_exists=reuse 时 diff platform/export_path，不匹配则纠偏或报错
- game_workflow_tools.gd：smoke_test_export 派生 preset/artifact_path；注册 run_export 的 output_path 工件

第五步：验证与收尾
- 全量 GUT（此时 CI 断言已可信）；本地跑受影响集成流（export/cache/动画门禁）
- 推送并跟踪 CI；诊断最新一次集成运行（泄漏修复后）仍红的残余原因
- changelog 记录

约束：4.7 优先/4.6 仅适配——所有改动不引入数字版本分支，沿用能力探测；不扩工具目录，无架构变更。