## Unreleased

- Cache observability: new supplementary `get_cache_diagnostics` tool (Debug-Advanced) exposes the result cache's hit/reuse rates, capacity and byte pressure, single-flight merges, tool-list/read-snapshot caches, external-change invalidation and spill counters — the telemetry existed but was internal-only, so the hit rate the whole cache design serves was invisible to clients and users. Deliberately not served from the result cache (the counters move on every call). Catalog 231 → **232 tools = 28 core + 198 advanced + 6 meta**; Debug category 73 → 74.
- First-run scan costs, not just repeats: a shared per-file memo (keyed on mtime+length, an autoload/global-class environment signature and the warnings variant; bounded 1024 entries) now backs `verify_scripts` and `detect_broken_scripts` compilation AND resource-dependency parsing — after any script/resource revision advances, a rescan recompiles/reparse only changed files instead of the whole project. Length in the key covers same-second rewrites; the environment signature invalidates when an added autoload or global class changes what "compiles" means.
- Consistent scan scope: `detect_broken_scripts`, both dependency scans, `search_in_files` and the symbol-reference walk now default to user code — addons/, test/ and docs/ are excluded (generated domains like .godot/ are never scanned). An explicit `include_tooling=true` or a search_path that itself points into a tooling domain opts back in; one audit report no longer mixes scopes (previously scripts were user-code-only while dependencies still parsed all 100+ addon files, and search/reference walks descended .godot/ and __pycache__ with zero-match patterns still opening every file). `search_in_files` also gains a documented `max_files` bound (default 2000) and a `files_available` count.
- Symbol-tool waste: `find_script_symbol_references` reads each file once (the definition pass and the match pass shared nothing; every file was read twice and the identical symbol RegEx recompiled per file), computes definition lines only for files that actually match, and skips no-match files before any comment stripping; `list_project_script_symbols` splits each file once and compiles its five literal C# RegExes once per module instead of once per file.
- Schema visibility: `insert_animation_keys` documents `attach_player_node`/`animation_name` (the handler and workflow runner already used both; the changelog announced the feature — but neither parameter appeared in the published schema); a `set_anchor_preset` step with a caller-supplied node_path and no preset no longer stalls on needs_input (CENTER derives in both branches).
- Gate honesty, round two: `inspect_gltf_asset` as an objective gate requires mesh_count > 0 (the handler reports success for an empty asset, so a failed text-to-3D generation looked like a delivered model); `generate_3d_asset` results register their `resource_path` as a model artifact that `inspect_gltf_asset`'s required `path` derives from; non-movement `play_and_verify` steps derive a 600 ms boot-settle window (zero steps returned before startup errors could reach the debugger bridge); a tool status of `unsupported` is now negative evidence for gates AND non-gate steps (it previously counted as usable evidence that a requirement was met).
- Localizations extraction walks the project once instead of twice and skips tooling directories so plugin strings never leak into the game CSV.
- Platformer-style goals actually plan now: `PROFILE_KEYWORDS` / blueprint verb tables gained platformer/jump/coin/collect/victory vocabulary (bilingual) — the plugin's own prompt example "2D platformer vertical slice" previously matched only `ui_screen` (via "screen") and never built a player, coins or win logic. Movement goals also expand the single default input-action step into four directional `move_*` actions bound to arrows + WASD (the blueprint controller reads all four; a lone `move_up` degraded movement to the `ui_*` fallback), and the CharacterBody2D scene-root derivation now triggers on any blueprint verb — a coins-and-win goal used to attach a CharacterBody2D script to a `Node` root and fail at attach time.
- Workflow blueprints survive JSON round-trips: integer literals in step arguments (keycodes) come back from disk as floats (`4194322.0`), which made `validate_integrity` judge the reloaded plan as tampered ("no longer matches the authorized blueprint") — the first numeric spec arguments hit this immediately. `_task_blueprint` now canonicalizes numeric leaf values, with a round-trip regression test.
- `create_export_preset` gains `if_exists` (`"error"` default / `"reuse"` idempotent success returning the existing preset with `status: "reused"`): the release_export profile replays its preset step on every replan, and the previous hard error on an existing name left that step — which has no repair tool — permanently stuck in `replan_required`.
- `save_scene` results register the scene artifact via their actual `saved_path` key: the mapping existed but the key list only had `save_path`, so the registry never recorded it and downstream `$scene` references stalled on `needs_input`.
- Three objective gates are falsifiable now: `get_runtime_animation_state` as a gate requires `is_playing` plus a non-empty `current_animation` (its payload has no pass/fail field, so it fell through to "data non-empty" and passed even when nothing played); `play_and_verify` derives input-exercise steps (press/release all four directions) for movement goals so the controller's `_physics_process` actually runs and its script errors surface — empty steps only proved "boots without crashing"; and a successful gate repair requeues the profile's completed screenshot step, so the visual gate re-captures instead of comparing the pre-repair candidate against itself.
- Cache hit-rate work: the workflow engine's hot whole-project reads are now in the shared result cache — `verify_scripts`, `detect_broken_scripts`, `list_project_script_symbols`, `find_script_symbol_definition/references`, `scan_missing/cyclic_resource_dependencies`, `audit_project_health`, `search_in_files`, `get_class_api_metadata`, `inspect_tileset_resource`, the export-preset reads and the test-discovery reads — tagged on `SCRIPT_AGGREGATE`/`RESOURCE_AGGREGATE` so any script/resource change invalidates them while unrelated domains keep their hits (never `SCRIPT_ALL`: `modify_script` deliberately does not advance it). `get_import_status` left the cache (it reports live scan progress — a time-domain value revision tags cannot track, and pollers were served a frozen `busy` for the whole TTL window); `list_unused_resources` gained the missing script tag (script preloads decide usage); `read_resource_properties` dropped its unnecessary script tag (unrelated script edits were killing per-resource hits).
- Main-thread cost fixes: `flush_tool_log` is append-only JSONL with a 4 MiB rotation cap (it reparsed and pretty-print-reserialized the entire accumulated file every 20 tool calls — O(N²) over a session, unbounded growth); every `get_runtime_*` call no longer walks the entire editor node tree (a `node_added` hook connects debugger windows immediately, the full walk is a 2s-throttled fallback with freed-debugger pruning); saturated capture buffers stop allocating a fresh n-element array per message (`remove_at(0)` instead of `slice()`); the HTTP transport thread sleeps 20 ms instead of 2 ms when fully idle (~500 wakeups/s → ~50); the self-heal watchdog re-reads its state file only when the mtime changes; `get_editor_logs` without a type filter parses only the requested window; HTTP `stop()` harvests the thread by `is_started()` (a thread exiting right before the check was destroyed unrealized).
- `inspect_export_templates` / `manage_export_templates` no longer abort in a real editor: `EditorPaths.get_export_templates_dir` does not exist on the tested stables (4.6.3 / 4.7.2, verified against ClassDB) and the unguarded call crashed the handler; it is now `has_method`-probed with the platform fallback intact, and the engine-contract test asserts the real contract instead of the false "4.7 provides it" premise.
- Dead code removed: `utils/node_utils.gd`, `utils/resource_utils.gd`, `utils/script_utils.gd` (276 lines, zero references); integration tests are portable via `GODOT_EXE`/`MCP_PORT` env vars with a sequential `run_integration.py` runner that assigns each test its own port, and the goal-flow scratch project is gitignored.

## [1.1.0] - 2026-08-31
- Goal matrix complete: all 12 production profiles now have goal-level coverage in one scratch project (11 reach `completed`, release_export reaches its honest boundary). This batch mined and fixed: `insert_animation_keys` gains `attach_player_node` wiring (adds the animation to an AnimationPlayer's default library in the edited scene); the animation_audio profile builds its own scene + AnimationPlayer and saves before the runtime chain, and runtime animation/audio steps derive their probe paths (`/root/<scene>/AnimPlayer`, Master bus); `run_project` joins the scene-scoped tools so the artifact scene is what actually runs; `reimport_resources` derives the goal's produced .tres/.tscn resources from disk; release_export creates a Windows Desktop preset when the project has none; empty-string `error` fields (a tool habit) no longer count as failures in gate verdicts; run_export failure receipts now carry the first error line instead of a bare `success:false`.
- Mid-step crash regression: `test_game_goal_crash_midstep_flow.py` kills the editor WHILE a step is dispatched (the persisted plan shows the in-progress task, caught by polling), restarts and asserts fail-closed semantics — a replay-safe uncertain step auto-recovers and the goal completes; a non-idempotent uncertain step must be reported explicitly ("unknown outcome ... inspect and replan", naming the step) with replan-then-run still finishing the goal. Across four runs the kill consistently landed mid-`save_scene` (idempotent-tagged) and the goal completed 25/25 every time. Shared test helpers (`rpc_call`/`tool_call`/`wait_for_server`) now accept an `mcp_url` override for port-isolated test variants.
- Resume-after-crash is now proven end-to-end: `test_game_goal_resume_flow.py` advances a goal one bounded slice, hard-kills the editor (process kill, no cleanup), restarts it on the same scratch project and drives the same plan to `completed` — same workflow id, no progress regression, completed-step count preserved through the crash (6 done before kill → 25 done at completion, verified twice). The checkpoint chain (per-step persisted plan, idempotent-safe replay, artifact registry) survives the restart without the model re-reading the project.
- The goal-level regression now runs two scenarios: the playable minimal game (25 steps, asserts real movement/pickup/win code and a CharacterBody2D root) and a performance-and-health goal (13 steps) whose fps/memory/no-runtime-error gates collect real runtime evidence on the game the first scenario just built — including in-editor restart of the same scratch project. Mining the second scenario added two derivation fallbacks: `read_script` without a script artifact falls back to the first script under `res://scripts/` (disk truth), and `assert_performance_budget` without a `budget` derives a conservative `{"min_fps": 30}` that callers can override.
- Goal-aware code blueprints: a fresh gameplay goal now produces a playable result instead of a placeholder. When the objective mentions movement / collectibles / a win condition (bilingual keywords), `create_script` steps derive a real controller (InputMap + arrow-key movement, a runtime-spawned collectible Area2D with pickup logic, a win label with a `You Win!` condition) and `create_scene` derives a `CharacterBody2D` root so the controller can actually move; explicit caller-provided `content` always wins. The goal-level integration test now asserts the generated controller contains `move_and_slide`, real pickup logic and the win path — the closed loop is verified end-to-end as playable, not just mechanically complete.
- Honest failures for silent tool aborts: a handler stopped by a GDScript runtime error used to return an empty `{"..."}` success; it now returns `isError` with "aborted without a result" pointing at the editor log.
- Scene activation now waits for the editor: `open_scene_from_path` takes effect on a later frame, so the scene-context guard's immediate re-check intermittently failed ("Failed to activate scene") right after `create_scene`. The guard polls frames until the switch lands (callers `await` it), which also stabilizes `create_node(scene_path=...)` flows.
- `attach_script` (standalone and inside `batch_scene_node_edits`) now guards against Godot's cold-resource trap: `load()` on a just-written script file returns an uncompiled shell (source present, no members, `can_instantiate() == false`) until the editor scans it — and a full scan can re-invalidate it mid-flight. The batch path compiles an equivalent in-memory script, re-applies it after `save_scene` (which swaps the node's script back to the path cache), and reports `bound` per `set_property` so non-`@export` script variables — which never bind on editor scene nodes — are called out instead of silently ignored. The whole chain is locked by an integration test (`test_batch_scene_node_edits_flow.py`) that assembles a scripted enemy in one call and verifies position/script/speed/signal on the live node plus the saved scene file.
- One-transaction scripted-node assembly: `batch_scene_node_edits` operations now include `set_property`, `attach_script` and `connect_signal` plus a `save` flag, so a create → configure → attach → connect → save chain costs one transactional call instead of up to five round trips (and still undoes in a single editor step). Nodes created earlier in the same batch are addressable via their `parent_path`/`node_name` path; signals and methods declared by an in-batch attached script are validated against that script before it is even set. Shape validation runs before the editor-environment check, so malformed batches are rejected (and unit-tested) headless.
- Two new zero-schema-cost executable prompts: `iterate_play_verify` (play → observe → gate on no-runtime-errors / performance / scenario conditions → smallest-fix loop with a three-identical-failure stop rule) and `release_export_flow` (templates → preset validation → version bump → export → launched smoke test → report). Both render defaults for optional arguments; the prompt catalog now numbers nine.
- `modify_script` inline validation: `.gd` content is validated after writing (`validate: false` skips) and the response carries a `validation` summary with the first errors, collapsing the edit → verify round trip into one call. Reuses the `validate_script` parser — no new validation logic.
- Export-template downloads are reliable again, and five stacked failure modes were fixed: the parallel probe getter never called `start()` (so once the editor had run for over 60 s every probe "timed out" on its first frame without ever opening a socket — netstat showed zero outbound connections); `_fail_all` cascaded through synchronous worker signals and rewrote every real worker error into a meaningless "coordinator cancelled"; a failed parallel attempt deleted the partial `.tpz` while keeping `.download.json`, leaving retries dying with "Cannot open destination file"; stale attempts' late `completed` signals (including unbound pre-hot-reload connections) marked fresh attempts failed; and an unfocused/occluded editor could stop iterating the scene tree, stalling the node-driven fetch. Downloads now temporarily enable the editor's *Update Continuously* setting (restored on finish/cancel), stale coordinator/getter nodes are swept before a fresh attempt, completion callbacks verify their source and phase, the self-heal watchdog also rebuilds a transport whose heartbeat reads zero (the hot-reload residue that kept port 9080 zombie-bound), a new `net_diag` action dials hostname / IP-literal / control targets with `_process` tick and frame counts to separate DNS, outbound-block and stalled-processing causes, and connections default to 8 with a cap of 16.
- Export presets gain atomic CRUD (properly integrated from the preserved WIP work, catalog 226 → **231 tools = 28 core + 197 advanced + 6 meta**): `inspect_export_presets`, `create_export_preset`, `update_export_preset`, `duplicate_export_preset`, `remove_export_preset` operate directly on `export_presets.cfg` with validation and read-back verification (`verified`), closing the last capability gap in the goal → export chain (`inspect_export_templates` → `manage_export_templates(download)` → `create_export_preset` → `validate_export_preset` → `run_export` → `smoke_test_export`) without `execute_editor_script` fallbacks. Round-trip unit tests included.
- Honest run semantics and evidence: `run_project` now re-checks the debugger session after a short grace window and reports `started_but_exited` (with guidance to collect error evidence) instead of a false-positive success when the game dies to a parse error right after launch. Workflow receipts keep quantitative gate evidence (`assert_performance_budget` → fps/p1_fps/p95_frame_time_ms; `assert_visual_baseline` → diff_pixel_count/diff_ratio; tests → passed/total), so replans and audits can see which metric failed instead of a bare `completed`.
- Fixed a real schema bug: `connect_signal`'s flags description claimed `2=ONE_SHOT, 4=PERSIST` — Godot 4's ConnectFlags are `1=DEFERRED, 2=PERSIST, 4=ONE_SHOT, 8=REFERENCE_COUNTED`. Tools could succeed while the agent reasoned about inverted semantics.

- Reliability salvage (reviewed subset of an unreviewed working-tree batch; the rest is preserved on `wip/reliability-core` for a dedicated integration pass): property writes now read back and self-verify — `update_node_property` and the runtime property tools report `requested_value` vs actual `new_value` plus a `verified` flag and accept `require_verified` to fail on clamped/ignored writes instead of reporting a fake success. Runtime node method/property arguments support self-describing Godot types (`{"__godot_type": "Vector2i", "x": 30, "y": 10}`), so structured values reach methods as real Vector2i/Color/Transform types instead of plain Dictionaries. `assert_performance_budget` gains a sampling window: `warmup_seconds` + `sample_seconds` + `sample_interval_ms` collect a stable window and gate on percentiles (`min_p1_fps`, `max_p95_frame_time_ms`) instead of one instantaneous reading that a single shader-compile hitch could fail. Project resource scans skip generated/import caches (`.godot`, `.import`, `build`) and can include/exclude tooling directories via the new `path_normalizer` / `generated_cache_filter` utilities.
- Export templates can now be downloaded end-to-end by the MCP itself. `manage_export_templates` gains `download` / `download_status` / `download_cancel`: the download runs in the background from an allowlisted official mirror (github / tuxfamily / godotengine; URLs are version-derived — the caller cannot pass arbitrary hosts), optionally through a loopback HTTP proxy (an explicit `proxy` param or the auto-detected system proxy; only loopback is accepted). Because Godot's `HTTPRequest` has no proxy support and GitHub release assets are unreachable without one on some networks, the tool ships a hand-written HTTPS client (`TemplatesHttpGetter`) supporting direct connections, HTTP CONNECT tunnels with full TLS verification by domain, redirects and chunked transfer decoding. Integrity is cross-checked against the official asset byte size from the GitHub release API (official releases publish only SHA-512 sums, which the engine cannot hash natively) — a fetched size is always enforced, and `require_integrity=true` fails closed when metadata is unreachable. On success the archive is auto-installed (existing extraction path) and the temp `.tpz` removed unless `keep_archive`. This is the tool-side trusted network path — `ScriptSandbox` restrictions on `execute_script` never apply. Verified live against api.github.com (direct, chunked) and a GitHub release asset through a Clash CONNECT tunnel (302 redirect + streamed body).
- Script writes are now editor-internal operations: `modify_script` and `create_script` sync the editor immediately after writing to disk (EditorFileSystem `update_file`, plus reloading the script's buffer when it is open in the Script Editor). Previously the raw `FileAccess` write was indistinguishable from an external editor's change, so Godot popped the "files changed on disk — reload?" confirmation dialog whenever the AI edited a script that was open in the editor. Results now include a `buffers_synced` field (`ok` / `skipped` headless) so callers can see the sync outcome.
- Fewer intermittent 502s behind the Cloudflare Quick Tunnel: every HTTP response now declares `Connection: close` before the server's intentional disconnect — previously keep-alive proxies (cloudflared's origin pool) could reuse a socket the server was about to close, surfacing in-flight requests as intermittent 502 Bad Gateway. The tunnel origin URL is now an explicit `http://127.0.0.1:<port>` (Windows may resolve `localhost` to `::1` first while the MCP server listens on IPv4 only), and persisted session validation accepts both the new and legacy `localhost` forms so existing tunnels remain adoptable. Response headers moved into testable `json_response_header` / `error_response_header` helpers with unit coverage.
- Scene-context guard for multi-scene goals: scene-scoped tools (`create_node`, `update_node_property`, `delete_node`, `set_anchor_preset`, `attach_script`, `save_scene`, `set_tilemap_layer_cells`) accept an optional `scene_path` that activates exactly that scene before writing (the previous scene is saved first when modified), eliminating silent cross-scene writes when several profiles create scenes in one goal. The workflow runner derives it from a per-profile scene artifact (`scene:<profile>`), `open_scene` is now idempotent (re-opening the active scene is a success no-op), and plan-authorized `open_scene` passes `allow_ui_focus` automatically. Visual gates close the loop: the runner derives `assert_visual_baseline`'s `candidate_path` from the latest runtime screenshot artifact and a deterministic `user://visual_baselines/` golden location — the tool's existing first-run golden capture then passes, and later runs compare against the stored image for real regression detection. No new tools were added.
- Workflow reliability (stress-test findings): tool evidence no longer misjudges real successes — `manage_localization` results carrying an explicit `success: true` pass without a status word, and mutation completions such as `created`/`saved`/`written`/`unchanged`/`recovered` are accepted by the generic verdict instead of triggering repair/replan. Successful creation steps (scene/script/theme/tileset/animation, test directory, smoke test) register a workflow artifact registry, and the runner resolves `$scene`-style references and auto-derives schema-required `script_path`/`scene_path`/`tileset_path`/`theme_path`/`animation_name` inputs from it, so `create_script → attach_script`-style chains no longer stall on `needs_input`. Objective-gate steps support negative testing via the new `expect_fail` plan option (`{"verify_scripts": true}` inverts that gate's verdict), enabling fault-injection loops that must prove a detector fails. Negated intent is respected: “Android 不适用” / “3D not applicable” no longer selects the android platform or adds the glTF inspection gate. `run_project` is idempotent — re-running the live scene (or omitting `scene_path`) reuses the session instead of erroring on “already running”, while a different scene switches sessions deterministically; plan-authorized `run_project`/`stop_project` auto-pass the interactive window policy. TileMapLayer cell tools name the active scene in node-not-found errors so editor-context drift is immediately actionable. No new tools were added.
- QA Bootstrap for game workflows: `prepare_project_test_environment` inspects `res://test/`, `res://tests/` and `res://.mcp_runtime_tests/` and returns `ready`/`empty`/`unconfigured`/`blocked` with a `recoverable` flag and recommended action. `ensure_project_directory` safely and idempotently creates `res://` directories, and `create_project_smoke_test` writes a framework-independent native smoke test so an empty project can pass QA without GUT or Python. `list_project_tests` now reports a missing directory as a recoverable `unconfigured` state instead of a fatal error. The `quality_assurance` workflow profile bootstraps the environment first (prepare → ensure directory → discover ≥1 test, auto-creating a smoke test when empty → verify scripts → run tests), so a fresh prototype no longer stops at `replan_required` when `res://test/` does not exist. Catalog: 226 tools (28 core, 192 advanced, 6 meta).
- Fixed the persistent Quick Tunnel startup regression: a detached headless supervisor now preserves the previously reliable stdout/stderr URL capture path while surviving editor shutdown, mirrors output into the project-scoped durable log, selects HTTP/2 for QUIC-blocked networks, and replaces failed or 20-second-stalled pre-URL children for up to three attempts. Once a URL is published it has no startup deadline and stops only on an explicit user request or process/computer exit. The panel's 30-second threshold is now a non-destructive slow warning instead of killing a live tunnel. Windows PATH reuse is normalized to remove mixed `\\/` separators, and DNS, proxy, TLS, rate-limit, timeout and Cloudflare-service failures receive localized actionable status text.
- The persistent-tunnel panel now keeps its status text synchronized with the selected UI language, paints visible local-cache and download-progress stages, automatically switches a stalled or unusually slow cloudflared download to the next verified source, warns when Quick Tunnel URL discovery exceeds 30 seconds without killing the process, supports cancelling an in-progress download with **Stop tunnel**, and immediately records source attempts, process launch, restore, slow-start and exit diagnostics in `mcp_server.log` instead of appearing to wait indefinitely.
- User-started Cloudflare Quick Tunnels now survive project reloads, plugin reloads and full Godot shutdown. The manager launches `cloudflared` as an independent process with a durable project-scoped log and session record, restores the same PID and public URL when the project reopens, prevents duplicate tunnel launches, validates the process command line before adopting or stopping a saved PID, and clears stale state after computer shutdown. Only the panel's explicit **Stop tunnel** action terminates a live managed tunnel.
- Cloudflare Quick Tunnel setup is now local-first across projects. The panel checks an always-visible manual override, the editor process `PATH`, a machine-local shared cache, and checksum-valid legacy `user://cloudflared` caches (including sibling Godot projects) before making any network request. Verified legacy files are copied atomically without deleting the original, managed macOS archives are rechecked on reuse, and only one shared download is needed per OS user, pinned version and platform architecture.
- Complete-game execution is now adaptive instead of functionally capped. Default checkpoint slices grow through 4/8/16/32 calls and a positive `max_steps` affects only the current call; a 100-capability regression proves every explicit requirement survives, executes and reaches evidence-gated completion without adding visible schemas. Unfamiliar composite goals can merge schema-free clause routes beyond ten capabilities, while uncovered requirements return clarification instead of false completion. Async work never blocks merely because it exceeded a poll count, identical receipts aggregate, distinct evidence is retained, transient failures return exponential backoff guidance, and repeated identical verification/repair failures request a different plan. On restart, audited reads and idempotent operations replay safely while uncertain mutations fail closed. Default responses expose only a four-step ready projection and counters rather than repeating the DAG, arguments, receipts or schemas.
- Added `plan_game_workflow` and `run_game_workflow`, bringing the catalog to 223 tools (28 core, 189 advanced, 6 meta). The deterministic compiler composes 12 reusable production profiles into a persistent goal DAG that can exceed the 8/10 ad-hoc discovery budget. Distinct same-tool writes retain separate profile/step identities; repeated reads reuse normal caches. Missing fields return the current atomic input Schema on demand without loading the catalog. Async polling, compare-and-swap IDs, protected paths and exact blueprint integrity prevent runaway or stale execution. Unknown goals and missing capabilities block explicitly, and `completed` requires engine-issued evidence for every objective gate. Gates cover 25 bilingual goals with 100% declared profile/capability recall, compilation P95 below 5 ms, plus a multi-round gameplay + UI + QA completion loop.
- Workflow routing now uses precomputed atomic-name signatures, bilingual intent aliases, direct action/object evidence, rare-term weighting and read-family matching before its cost-aware fallback. A deterministic gate covers 48 real English/Chinese game-production tasks across 12 domains and 174 required atomic-tool groups: Recall@8 and complete-task success are 100%, verification-stage recall is 97.30%, known cross-domain distractors are 0, and average supplementary-schema savings are 97.34%. The default budget remains 8, every atomic tool remains exactly addressable and no tool, schema, model call or network dependency was added.
- High-volume read pagination is now lossless and compute-aware. `list_project_resources`, debugger stack frames/variables, reverse-resource scans and migration/deprecation scans expose common continuation metadata (`returned_count`, `has_more`, `next_offset`); stable project scans reuse one dependency-revisioned full snapshot across page views instead of rescanning. Snapshot retention is bounded to 8 entries and 4 MiB each. Stateful `apply_migration_fixes` is intentionally not re-executed for pagination, so functionality and consistent mutation semantics remain ahead of token/latency savings. This change added no tool.
- Large successful tool results are now cheap up front without becoming lossy: the existing head/tail preview includes a standard MCP `resource_link`, and `resources/read` serves the exact JSON in deterministic UTF-8-safe 16 KiB pages addressed by SHA-256. Identical payloads reuse verified spill files, dynamic links do not bloat `resources/list`, errors and source/log readers remain complete inline, and spill failures fall back to the complete inline response. A 204 KiB Unicode regression fixture verifies 95.62% fewer initial bytes and exact 13-page reconstruction.
- `set_dod` now rejects whitespace-only criterion text on the single-criterion path (both creating a new criterion by text and renaming an existing one by index), returning an error instead of persisting an empty-text criterion. This matches the non-empty rule already enforced on the full-list / `add_task` path.
- Made DoD criterion text consistently trimmed on every storage path. `set_dod` now stores the trimmed `criterion` (matching how criteria are matched and created), and `_normalize_dod` (used by `add_task` and full-list `set_dod`) now trims string entries and rejects whitespace-only ones, the same as dict entries. Previously a `criterion` passed with surrounding whitespace (e.g. `"  fps ok  "`) could be persisted with padding yet matched trimmed, causing lookups to miss and duplicate entries to appear.
- Performance: cut redundant work on the per-request hot path without changing any behavior. The script sandbox (`execute_script` / `execute_editor_script` / `evaluate_*` guard) now compiles each denylist RegEx once and reuses it from a process-lifetime cache instead of recompiling ~26 patterns on every scan, and strips string/comment content with an O(n) buffer join instead of O(n²) per-character concatenation. The JSON-RPC server no longer builds full-request/response `JSON.stringify` strings for debug logging when the log level is above DEBUG (the default), so every `tools/list`, `tools/call`, `resources/list`, and incoming message skips that wasted serialization.
- Made `set_dod` single-criterion updates transactional: the new-criterion path validates all error conditions (invalid gate, `observed` without a gate, non-dict `observed`) before appending, and the existing-criterion path mutates a copy that is only committed once every check passes. A failed call no longer leaves a half-created criterion or a partially-modified one (e.g. a gate attached but the subsequent `observed` evaluation rejected) in the task's DoD.
- Hardened node-creation type validation in `create_node` and `batch_scene_node_edits` create ops: they now reject non-Node classes (e.g. `Resource`, `RefCounted`) and abstract Node classes (e.g. `CanvasItem`) up front with a clear, actionable error instead of letting `ClassDB.instantiate` return null and crashing on the subsequent property assignment. The check is a pure `ClassDB`-only helper (`class_exists` → Node-derived → instantiable), so it's deterministic and unit-tested. Concrete Node types are unaffected.
- Tightened `manage_task_plan` DoD gate evaluation: `set_dod` now evaluates `observed` against the gate even when it creates a brand-new criterion by `criterion` text (previously the metrics were silently ignored and `met` defaulted to false on that path), and an invalid gate no longer leaves a half-created criterion behind. A `no_runtime_errors` gate now requires an actual measurement — an empty `observed` is treated as "can't prove ⇒ not met" instead of passing on a default of 0, matching the other gate types. Also refreshed the `manage_task_plan` tool description in the translation files and documented the optional `gate`/`last_evaluation` DoD fields in the persisted schema.
- Closed a script-sandbox bypass and reduced false positives: `execute_script`'s single-line Expression path is now scanned by the same guard (previously only the multi-line/`execute_editor_script` path was), so a single-line `OS.execute(...)` can no longer slip through under STRICT security. The filesystem check no longer flags Godot scene-tree node paths (`/root/...`) and only treats `~`/`~/` as a home-dir path instead of matching any string containing a tilde (e.g. `"~5 enemies"`); out-of-project absolute and system paths (`/etc/`, `/var/`, drive letters, `~/...`) stay blocked.
- Hardened `manage_task_plan` Definition-of-Done with optional machine-checkable `gate`s so the VERIFY phase decides `met` objectively instead of self-asserting. A DoD criterion can declare a gate of type `performance_budget` (a `budget` of min_fps/max_frame_time_ms/max_physics_frame_time_ms/max_object_count/max_resource_count/max_rendered_objects/max_memory_mb/max_node_count, mirroring `assert_performance_budget`), `no_runtime_errors` (`max_errors`, default 0), or `visual_baseline` (`max_diff_pixels`/`max_diff_ratio`). On `set_dod`, passing `observed` measured metrics auto-computes `met` from the gate and records the verdict as evidence. No new MCP tool; this deepens an existing one and the catalog stays at 215 tools.
- Added a script sandbox guard for `execute_editor_script`, `evaluate_debug_expression` and `evaluate_runtime_expression`: under STRICT `security_level` (the default), scripts/expressions are scanned by a configurable capability denylist (OS process execution, out-of-project filesystem paths, networking, other dangerous APIs) and rejected with a structured `blocked` result before execution. PERMISSIVE mode keeps the previous unrestricted behavior. No new MCP tool is added; the guard hardens existing tools and the catalog stays at 215 tools.
- Expanded the MCP tool catalog to 215 tools by adding the ship-loop closure and localization tools.
- Added `smoke_test_export` (Editor-Advanced): post-export smoke test that resolves the artifact, optionally exports first, asserts the product file exists, and launches it to capture and check the exit code — an objective, runnable-build gate.
- Added `bump_version` (Project-Advanced): semantic version bump written back to project.godot plus an automatic dated changelog entry, with a `dry_run` mode.
- Added `manage_localization` (Project-Advanced): single action-based localization workflow — `list` registered `.translation` files, `extract` translatable keys from scenes/scripts into a Godot CSV (preserving existing translations), `import` that CSV into per-locale `.translation` files registered in ProjectSettings, and `export` registered translations back to CSV for round-trip checks. All write actions support `dry_run`.
- Added external-generation budget guards: `generate_asset` and `generate_3d_asset` external calls now honor a configurable call-count/limit window (`external_gen_budget`, `external_gen_budget_window_sec`) and reject calls over budget. Default 0 keeps it unlimited and backward-compatible.
- Added a minimal GitHub Actions CI workflow (headless import + GUT unit tests).
- Refined and normalized the documentation set: root README, Chinese README, addon README files, configuration, architecture, testing, contributing, remote access, industrialization guides and generated tool-reference pages.
- Repaired corrupted Chinese documentation text in repository-facing docs and agent skill guides.
- Clarified the 215-tool model: 30 core tools, 183 advanced tools and 2 always-on meta tools.
- Security hardening (Phase 0): the HTTP transport now binds to `127.0.0.1` by default (`allow_remote` must be explicitly enabled to bind all interfaces), the CORS origin defaults to empty (no CORS header), JSON-RPC batch payloads return `-32600` instead of crashing, and connections are capped. `execute_script` / `execute_editor_script` were downgraded from core to supplementary (disabled by default) to shrink the default remote-code-execution surface; `generate_asset` / `generate_3d_asset` now validate endpoints (https + host allowlist) and API-key environment variables. Tool counts updated accordingly: 28 core + 185 advanced + 2 meta = 215.
- Protocol compliance (Phase 0): `ping` is now handled, notifications never receive responses, rate limiting skips notifications, and `tools/list` / `resources/list` / `prompts/list` responses include `_meta.ttlMs` / `cacheScope` (required by current MCP clients). `serverInfo.version` and the HTTP info endpoint now read the real plugin version from `plugin.cfg`. Auth can now be toggled at runtime (auth manager is rebuilt on server start from current settings). `get_editor_logs` no longer depends on translated UI panel names and falls back to the versioned editor log file and `user://logs/godot.log` script-error lines. `validate_script` now returns structured compile errors with line numbers extracted from the compiler output.

## 1.0.7-pre1 (current)

- Expanded the MCP tool catalog to 212 tools.
- Added `generate_3d_asset` (Project-Advanced): bring-your-own-key text-to-3D generation that submits a job to an external provider (meshy/tripo presets), polls until completion, downloads and validates the glTF/GLB into `res://`, and inspects the result by default.
- Added asset-closure workflows including sprite-sheet slicing and glTF/GLB inspection.
- Added deterministic `play_and_verify` workflows for frame-stepped playtesting and game-feel metrics.
- Added regression gates for visual baselines, performance budgets and runtime errors.
- Improved project-level automation around task plans, resource dependency checks, migration scans, TileSets, render output and generated assets.

## 1.0.6

- Improved runtime/debug tooling and project inspection workflows.
- Expanded integration coverage for runtime probe and editor automation flows.

## 1.0.3

- Stabilized the native Godot MCP server architecture.
- Added core scene, node, script, editor, debug and project tools.
- Added HTTP/SSE and stdio transports.
