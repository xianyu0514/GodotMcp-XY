---
description: AI 自主开发与自动提交规则 — 自动分支提交、每次提交保持互相兼容、按任务持续推进不询问、用最省算力的分层测试保证功能。
alwaysApply: true
---

# AI 自主开发规则（Autonomous AI Development）

本规则定义 AI 在本仓库（Godot MCP Native）中如何**自主、连续、低算力**地推进开发并自动提交。
目标：在不打断用户、不浪费算力的前提下，让每次提交都能编译、功能可用、且与其他模块互相兼容。

## 0. 优先级与适用范围

- 本规则与 `AGENTS.md` 同为**用户明确授权**的最高优先级指令。
- 用户已明确授权 AI **自动提交分支**——本规则取代任何"未经允许禁止 git 操作"的旧约束。
- 适用于所有由 AI 发起的代码/文档改动。

## 1. 持续推进，不中途询问

- 把任务拆成可独立完成的小步，**逐步做完整条链路，中途不要停下来征求意见**。
- 遇到歧义或多种实现可选时，**默认选择最保守、最向后兼容、最省算力的方案**继续，不要为了"确认一下"而阻塞。
- 只有在**真正不可恢复的硬阻塞**时才停下并告知用户（见第 6 节）。
- 不要因为"任务很长/很重复"而提前收尾；持续做到完成。必要时用 `checklist.txt`（不提交）跟踪进度。

## 2. 自动分支与提交

- **永远在功能分支上工作，绝不直接提交到 `main`。**
- 分支命名沿用仓库约定：`devin/<unix-timestamp>-<topic>`（例：`devin/1782231254-autonomous-rules`）。
- **小步提交**：每完成一个能独立工作、且通过第 4 节兼容性门禁的小步，就提交一次。
- 提交信息用 Conventional Commits：`feat:`、`fix:`、`test:`、`docs:`、`refactor:`、`chore:` + 简洁说明（中/英皆可）。
- 提交前确认 `git status` 只包含有意改动的文件：
  - **禁止**提交 `.godot/`、`.import/`、`*.translation`、`user://` 配置、token、编辑器缓存、`.codeartsdoer/temp/`、`.tmp_*` 等临时/生成文件。
  - 不要 `git add .`；只 add 明确相关的文件。
- 完成一个完整任务后，推送分支并开 PR（PR/合并流程见 `.cursor/skills/pr-review-merge`）。
- 严禁强推 `main`，严禁跳过 hook，严禁 `git reset --hard` / `git clean -fd` 等破坏性命令（除非用户明确要求）。

## 3. 每次提交都"互相兼容"的含义

每次提交都必须保证**不破坏项目其他部分**：

1. **项目可干净导入**（最关键、最便宜的门禁，见第 4 节第 0 层）：没有任何脚本解析/加载错误。
2. **向后兼容工具接口**：
   - 不删除、不重命名已有 MCP 工具；新增参数一律可选并带默认值，保持旧调用仍然有效。
   - 改 `input_schema` / `output_schema` 时保持对旧客户端的兼容。
3. **跨文件不变量保持同步**（这是最容易让模块"互不兼容"的地方）：
   - 工具数量：`mcp_tool_classifier.gd` ↔ `test_mcp_tool_classifier.gd` ↔ `README.md` / `README.zh.md` ↔ `docs/tools/*` 的计数必须一致。
   - 翻译：新增/改名工具同步 `translations/tool_descriptions.json` 与 `.csv`。
   - 分类：core 上限 30 不被无意突破；advanced 工具默认 `supplementary`。
4. **不留半成品**：不提交会让项目无法导入或让相关测试变红的中间状态。

## 4. 最省算力的分层测试策略（强制）

**核心原则：用能证明本次改动的最便宜的测试，能在低层验证就绝不上高层。** 不要"以防万一"跑全量测试。

按从便宜到昂贵分 4 层，**默认只跑满足本次改动的最低层级**：

| 层级 | 内容 | 命令 | 何时用 |
| --- | --- | --- | --- |
| **L0 导入门禁**（最便宜，每次提交必跑） | 让项目导入一次，抓解析/加载错误 | `godot --headless --path . --import`，然后检查输出无 `SCRIPT ERROR`/`Parse Error`/`Failed to (load\|parse) script` | 任何代码改动；文档改动也用它确认没误伤 |
| **L1 单文件单测** | 只跑与改动直接相关的那一个 GUT 测试文件 | `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://test/unit/tools/test_<x>.gd -gexit` | 单个工具的 handler / schema 改动 |
| **L2 子目录单测** | 跑受影响分类的子目录 | `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://test/unit/tools/ -ginclude_subdirs -gexit` | 影响一类工具或分类器/server core 的改动 |
| **L3 全量 + 集成**（最贵，按需） | 全套 GUT + 必要的 Python 集成测试 | 全套：`-gdir=res://test/unit/ -ginclude_subdirs`；集成：`python test/integration/<target>.py` | 仅在合并入 `main` 前，或改动跨编辑器/运行时/传输边界时 |

按改动类型选择最低充分层级：

- **纯文档/注释** → 只跑 L0（+ 必要时 JSON/markdown 校验），不跑 Godot 单测。
- **单个工具 handler/schema** → L0 + L1。
- **分类器 / server core / 跨类工具** → L0 + L2。
- **运行时探针 / 传输 / 认证 / 导入导出** → 相关项 L3（针对性集成测试，不是全量）。
- **开 PR 合并前** → 至少跑一次 L0 + 与改动相关的 L2/L3，让其对齐 CI（CI = L0 导入门禁 + 全量 GUT）。

省算力的硬性要求：

- **复用同一个 Godot 实例**，不要为每个测试反复冷启动编辑器。
- 没有 Godot 可执行文件时，用 L0 的静态等价物（解析/语法检查、JSON/CSV 校验、计数一致性检查）作为最低门禁，并在 PR 注明未能跑引擎测试。
- 不重复跑已经通过且未受影响的测试。
- 集成测试（启动编辑器 + HTTP）算力最贵，**仅在确有必要时**运行。

## 5. 改动后的同步清单（避免"不兼容"的常见来源）

- 新增/改工具：按 `AGENTS.md` 与 `docs/contributing.md` 的流程同步 handler、分类器、单测、翻译、文档、计数。
- 改完清理临时文件：`.codeartsdoer/temp/`、根与 `test/integration/` 下的 `.tmp_*`。
- 提交前 `git status` 复核，确保无意外文件。

## 6. 何时才停下（硬阻塞）

仅在以下情况停下并向用户求助，其余一律自主继续：

- 缺少必须的密钥 / 外部权限 / 凭据。
- 环境/基础设施反复失败（同一环境级错误重试 3–4 次仍失败）。
- 用户声明应存在的前提被证伪（按"证伪即上报"处理）。
- 某改动会与用户明确指令相矛盾。

## 6b. 制作游戏时按固定 playbook 跑

做"用 MCP 工具制作游戏"的任务时，每次按同一套可执行 playbook 推进，保证流程确定、可复跑：

- 规划：[GDD → 任务图 playbook](../../docs/industrialization/playbook-gdd-to-task-graph.md) —— 用 `manage_task_plan` 把 GDD 落成带**可机检 DoD 门禁**（`gate`）的任务图。
- 执行：[单切片 PLAN→EXECUTE→RUN→VERIFY→FIX playbook](../../docs/industrialization/playbook-single-slice.md) —— 取一个就绪任务跑完整闭环；VERIFY 阶段把实测值经 `set_dod` 的 `observed` 回填，由门禁客观判定 `met`，未过门禁不得 `set_status=done`（不要 `force`）。

## 7. 一句话总结

在功能分支上小步自动提交；每次提交先过 L0 导入门禁、保持向后兼容与跨文件计数同步；用能证明改动的最便宜测试；持续推进直到完成，只有硬阻塞才停。
