# P0 自校准报告 — N1 + E4（第一方模型）— 2026-09-13（第二次）

> 事件流：`runs/self_agent_n1.jsonl`（可重算）。接续 #137 的首份报告；本轮补齐
> 墙碰撞 oracle 并首跑 E4。**校准运行，不计入正式对照样本。**

## N1（含墙碰撞）— 通过

| 检查 | 结果 |
| --- | --- |
| 位移 > 15px | ✅ |
| 零运行时报错 | ✅ |
| **撞墙不穿透**（wall 动词蓝图建墙，长按右移断言 x < 240） | ✅ |

产品新增：**墙动词**（wall/墙，中英）——目标提"撞墙停止"即生成 StaticBody2D
边界墙（世界坐标延迟挂载），N1 oracle 三项全绿。

## E4 — 失败（两次尝试），根因已记录

| 尝试 | 失败点 | 根因 |
| --- | --- | --- |
| 1 | play 门禁左移腿（600ms 仅移动 ~39px） | **帧饥饿**：编辑器连续两场重负载，墙钟等待下物理帧不足 |
| 2 | play 门禁（玩家从 x=+810 起步） | **重规划复用了残留游戏进程**——绝对位移阈值在非原点起步必败；同时是代理侧协议违规（重规划应换新项目/先停游戏） |

**产品缺口（转入待办）**：
1. 重规划路径应在 re-run 前 stop_project（复用 #126 已知模式）；
2. 位移断言可改为步前快照式相对断言（inert 机制的反向），消除原点假设。

## 校准结论（进入正式 12×3 前必须）

1. 修复上述两个产品缺口；
2. 每任务强制 fresh scratch（协议要求，代理侧遵守）；
3. 帧饥饿缓解：任务间留 settle、或位移阈值乘负载余量；
4. R3 校准待跑。

**诚实声明**：E4 记为 fail——错误完成声明为 0（代理未谎报）；失败样本全保留。

## E4 复测（#139 修复后）— 通过

| 检查 | 结果 |
| --- | --- |
| 重命名符号运行时生效（gems_collected > 0 after sweep） | ✅ |
| 零运行时报错 | ✅ |
| 首过（零澄清零人工） | ✅ |

**修复验证闭环完成**：#138 测出失败 → 根因（脏状态 + 原点阈值）→ #139 修复 → 复测翻绿。
N1 + E4 校准均通过；R3 与正式 12×3 为下一步。

## R3 校准（崩溃恢复）— 部分通过（两次运行，根因已记录）

| 运行 | 工作流 | Oracle | 说明 |
| --- | --- | --- | --- |
| 1 | completed ✅ | 位移=0 ❌ | oracle 向右走撞上了目标生成的右墙（x=234，墙在 250）——oracle 设计缺陷，非产品缺陷 |
| 2 | waiting（repair 循环） | ✅ 通过 | oracle 改左移后验证通过；但工作流在崩溃恢复后卡在 play 门禁修复循环 |

**诚实结论**：崩溃恢复后**行为本身两次都被验证**（真机位移、零报错）；
工作流收口不稳定（run1 完成、run2 卡修复循环——帧饥饿下 play 门禁重试不收敛）。
**产品缺口**：崩溃恢复的 play 门禁在持续负载下可能进入修复循环不收敛——
与 E4 attempt1 同因（帧饥饿），但恢复路径没有自适应阈值缓解。
